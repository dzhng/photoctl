{
  "name": "@photoctl/workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "wb": "dist/cli.js" },
  "scripts": { "build": "tsc -p tsconfig.json" },
  "dependencies": {
    "@photoctl/library": "workspace:*",
    "@photoctl/protocol": "workspace:*"
  }
}

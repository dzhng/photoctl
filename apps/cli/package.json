{
  "name": "@dzhng/openphoto",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "openphoto": "dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@photoctl/commands": "workspace:*",
    "@photoctl/daemon": "workspace:*",
    "@photoctl/protocol": "workspace:*"
  }
}

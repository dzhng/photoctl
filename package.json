{
  "name": "photoctl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun run build:ts && bun run build:rust && bun run build:swift",
    "build:ts": "turbo run build",
    "build:rust": "cargo test --workspace --no-run",
    "build:swift": "if [ \"$(uname -s)\" = Darwin ] && [ -f helpers/mac/Package.swift ]; then swift build --package-path helpers/mac; else echo 'Swift build skipped (non-macOS)'; fi",
    "test": "bun run test:ts && bun run test:rust && bun run test:functional && bun run test:macos",
    "test:ts": "vitest run --config vitest.config.ts",
    "test:rust": "cargo test --workspace",
    "test:functional": "docker compose -f test/compose.yaml run --rm functional",
    "test:macos": "if [ \"$(uname -s)\" = Darwin ]; then vitest run --config vitest.macos.config.ts; else echo 'macOS tests skipped (non-macOS)'; fi",
    "lint": "oxlint .",
    "typecheck": "tsc -b --pretty false",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "wb": "node apps/workbench/dist/cli.js",
    "verify": "bun run fmt:check && bun run lint && bun run typecheck && bun run build && bun run test",
    "publish:npm": "node scripts/publish-npm.mjs",
    "version": "node scripts/sync-versions.mjs && git add apps/*/package.json packages/*/package.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "oxfmt": "^0.15.0",
    "oxlint": "^1.0.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "trustedDependencies": [
    "fs-ext"
  ]
}

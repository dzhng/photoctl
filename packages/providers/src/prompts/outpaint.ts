export const OUTPAINT_PROMPT_VERSION = 1;

export function outpaintPrompt(): string {
  return "Extend the image naturally into the masked canvas while preserving composition, lighting, and perspective.";
}

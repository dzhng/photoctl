# Remaining references and native portrait detail

This completes current-runtime saved-reference breadth alongside the ten-image
gold run. The eight remaining references pass native public show/export with matching
RAW source and render identities, applied LibRaw reconstruction at native scale,
and all 36 original hashes unchanged. No imports, edits, resets or builds occurred.
The [command evidence](evidence.json) identifies the same addon as the gold run;
[verification](verification.json) binds the reports and capture set.

Earlier ordinary RAW exports and current exports share develop state and zero layers.
The extra gold portrait crops use the actual preceding/current gold deliveries, not
different renders. Native crops retain their source pixels without resizing. All 54
PNGs and their hashes were independently verified and directly inspected by root,
full frames before detail. Fresh PNG-only reviewer `/root/remainder_critique` inspected
the complete set without code, measurements or project history.

Both inspections prefer the current B arm's reduced cyan/red edge speckle in lace,
coastal-portrait hair/glasses, rock and fine branches. Lace holes, stitches, pores,
hair direction, rock contours and elephant anatomy remain recognizable. Sky gradients,
orange lighting and yellow/orange bokeh remain materially alike. B is smoother in some
fine texture; these images do not prove every removed high-frequency variation was
false. Broad purple branch fringes and defocused doubling remain in both arms, with
their cause unestablished. Soft wildlife/glass regions and clipped window/lights also
remain; no newly displaced edge, large halo or global hue shift is visible.

Accept the interpolation correction within this evidence, not universal photographic
perfection or an attribution of the remaining fringes. Do not rerun these same breadth
comparisons to rediscover them: any further fidelity change needs a named defect,
causal evidence and its own behavioral regression. Complete JPEGs and reproduction
scripts remain at `/private/tmp/photoctl-balanced-remainder.yenEbU`.

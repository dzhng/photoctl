# Mounted-camera gold evidence

The prescribed command journey succeeded on the connected A7C II card through a
fresh packed installation. Photographic delivery acceptance remains open: command
success and intact files do not establish fidelity or a photographer's approval.

## Provenance and mechanical result

Source checkout: `ade0327`, with the preview correction at `a620217`. The normal
pack and clean-install scripts created an isolated prefix on this Mac, using the
previously approved temporary CMake/Ninja environment. Execution was outside the
checkout, through the installed persistent daemon, not Docker. Both native decoders
were available; embedding was manual and no gateway key or fake volume map was set.

The source was `/Volumes/Untitled/DCIM/101MSDCF`, volume UUID
`0051C01F-A0CE-36FC-9386-477DA323CD33`. The existing gold script used `--source-kind real`
and its ordinary linked, paired-default import. The
[unchanged command report](gold-exam-report.json) records 431 admitted logical photos,
ten successful ratings, three successful people-preset edits and ten successful
4608×3072 exports, without import conflicts or skipped exports. Each export records
its selected original identity; all ten match the RAW primary reported by `list`,
with one RAW and one JPEG member each. The [portable report](report.html) links the exact
delivered JPEGs; `shasum -a256 -c SHA256SUMS` verifies all ten plus both reports after
retention in this folder.

The scratch prefix, catalog and review material remain at
`/private/tmp/photoctl-mounted-gold.S2ToQ2`. The installed daemon was stopped and its
PID confirmed absent before handback. Camera operations were read-only; no source
write or metadata-sync command ran. No before/after whole-card hash comparison was
collected, so this is an action-scope statement, not a byte-comparison proof or a
claim about macOS mount metadata. No further camera access is needed to inspect
these retained deliveries.

## Visual verdict

All ten full frames were inspected. An independent, unprimed reviewer also inspected
all ten decoded full-frame PNGs and their fixed subject crops: x=2100, y=1550,
width=650, height=650 in the delivered JPEG, enlarged 2× with nearest-neighbor
sampling. Full frames were displayed at 1920×1280. The crops are reproducible from
the retained JPEGs; redundant decoded PNGs stay in scratch storage.

The reviewer found no obvious torn geometry, misplaced tiles, repeated regions,
rainbow colors or major corruption. Color was plausible at the inspected scales.
The sequence is dark and muted, especially frames 3–10; the rhino is small and blends
into vegetation. Frame 1 has conspicuous facial softness relative to nearby branches;
all crops have limited fine detail and some grain/mottling. Foreground twigs and
grass obscure parts of the animal. These are pixel observations, not an established
processing defect: without references they cannot distinguish capture softness,
exposure or noise from pipeline changes.

**Disposition:** mechanical mounted-drive journey accepted; no broad photographic
acceptance claimed. These are ten captures of one rhino scene, not ten representative
scenes or a before/after comparison. Do not tune rendering around this sequence or
replace the separate all-reference photographic review with it. The generated
report's `photographic_acceptance: "not_recorded"` remains unchanged.

## Remaining boundary

This run did not perform explicit camera-JPEG view/export from this same catalog,
the additional witness required by pairing slice D. Earlier mounted and installed
fixture tests prove that behavior only within their own scopes. The camera was
released before that gap was reconciled; do not claim this evidence closes it or
silently read the camera again. It also does not prove Lightroom Classic interop,
headless SSH, other platforms, live providers or a clean full-repository gate.

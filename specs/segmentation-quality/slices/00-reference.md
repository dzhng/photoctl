# 00 — Freeze the external contract

Question: can the port reproduce the accepted path without inheriting SAM2
assumptions? The reference source and ONNX versions are frozen in
the [reference manifest](../assets/zim-contract/reference.json). Large tensor fixtures stay local, with hashes
and deterministic regeneration instructions. Never redistribute the extracted
video source as a licensed product fixture.

The completed reference probe used one encode and four decodes: official
points, official box, official combined, combined adapter. Points reproduce the
accepted archived alpha bit-for-bit. Combined upstream drops all signed points;
the adapter keeps them and appends labels 2/3, without a sentinel when boxed.
Point attention remains upstream's point attention. This is numerical contract
evidence, not a claim of automatic combined-prompt quality.

Complete the real entire-person case with one automatic signed-grounding call
on the same source, then one encode/decode and score argmax. Include hair,
face, skin and clothing; exclude bouquet and background. Preserve all model
candidates and the gateway request/response without credentials. No hand edits
or retries selected by appearance. Missing credentials are recorded, not faked.

Human surface: full portrait and reference-viewport composites, plus crown,
face boundary, curls, clothing and flowers crops. compare-screenshots compares
membership and edge loss; unprimed screenshot-critique is the final visual gate.
Person is judged against its own scope, not a hair mask. Open with preview-shots;
continue independent work while allowing a short response window.

Delegated: scratch fixture packaging and report layout. Frozen: model/prompt
inputs, candidate rule, target scope. Production acceptance remains slice 06.
